import { print } from "../../src/lang/ast_printer";
import * as ast from "../../src/lang/ast";
import * as tok from "../helpers/token";

describe("print", () => {
  const num1 = new ast.Number(tok.num(12));
  it("should print numbers", () => {
    expect();
  });
});
